<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{ title }}</title>
</head>
<body>
    <h1>Hello {{ name }}!</h1>
    <p>The URL for the home page is: {{ url_for('home') }}</p>
    {{ name|upper }}
    <p>The URL for this page is: {{ url_for('hello', { 'name': name }) }}</p>
</body>
</html>
